query AllUsersQuery {
    allUsers {
        id
        email
    }
}

query Login($email:String,$pass:String) {
    login(email:$email,password: $pass) {
        responseCode
        responseMessage
    }
}


