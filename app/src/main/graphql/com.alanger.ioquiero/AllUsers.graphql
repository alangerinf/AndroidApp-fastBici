query AllUsersQuery {
    allUsers {
        id
        email
    }
}

query LoginQuery($email:String,$pass:String) {
    login(email:$email,password: $pass) {
        responseCode
        responseMessage
    }
}


